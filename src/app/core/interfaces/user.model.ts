export interface User {
    cliente_id: string;
    nombre: string;
    email: string;
    telefono: string;
}

export interface UserRegister {
    nombre: string;
    email: string;
    telefono: string;
}